apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: parameter-service
  name: parameter-service
  namespace: gateway-app
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    io.kompose.service: parameter-service